
import { flightStops } from 'flight-search/helpers/flight-stops';
import { module, test } from 'qunit';

module('Unit | Helper | flight stops');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = flightStops([42]);
  assert.ok(result);
});

