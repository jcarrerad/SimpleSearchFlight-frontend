
import { moneyFormatter } from 'flight-search/helpers/money-formatter';
import { module, test } from 'qunit';

module('Unit | Helper | money formatter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = moneyFormatter([42]);
  assert.ok(result);
});

